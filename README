o Create database of images
  o low-res
  o Artists
    o Picasso
    x Monet
    x Rembrandt
    o Pollock
    o Seurat
  o Feature extraction data
    o Serialize the data?
o Feature extraction
  x Color palette (group)
  x # blobs (hw)
  x SIFT (vlfeat)
  x Local Binary Patterns (ari)
  x Harris Corner Detection (hw)
  x Histogram of Oriented Gradients (ari)
o Classification
  o Bayesian Network
  o SVM (Support Vector Machine)

