o Create database of images
  o low-res
  o Artists
    o Picasso
    o Monet
    o Rembrandt
    o Pollock
    o Seurat
  o Feature extraction data
    o Serialize the data?
o Feature extraction
  o Color palette (group)
  o # blobs (hw)
    o OpenCV blob detection
    o average size of blobs
    o number of blobs
  o Null feature extraction
    o raw machine learning
    o simple classification based upon feeding the image to the classifier
  o SIFT (openCV)
  o Local Binary Patterns (ari)
  o Harris Corner Detection (hw)
  o Histogram of Oriented Gradients (ari)
  o Edge Detection (drew)
  o Geometric Probabilty Map
  o Texton Histogram (mike)
o Classification
  o Bayesian Network
  o SVM (Support Vector Machine)

